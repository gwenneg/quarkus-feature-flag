= Quarkus - Custom
:extension-status: preview

Describe what the extension does here.

== Installation

If you want to use this extension, you need to add the `io.quarkiverse.custom:quarkus-custom` extension first.
In your `pom.xml` file, add:

[source,xml]
----
<dependency>
    <groupId>io.quarkiverse.custom</groupId>
    <artifactId>quarkus-custom</artifactId>
</dependency>
----

[[extension-configuration-reference]]
== Extension Configuration Reference

include::config.adoc[leveloffset=+1, opts=optional]
